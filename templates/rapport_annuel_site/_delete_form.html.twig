<form method="post" action="{{ path('rapport_annuel_site_delete', {'id': rapport_annuel_site.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport_annuel_site.id) }}">
    <button class="btn">Delete</button>
</form>
