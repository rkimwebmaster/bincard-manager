<form method="post" action="{{ path('r_annuel_general_delete', {'id': r_annuel_general.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ r_annuel_general.id) }}">
    <button class="btn">Delete</button>
</form>
