{% extends 'base.html.twig' %}

{% block title %}Dispatch{% endblock %}

		{% block titre %}
				Liste des dispatch  {% endblock %}
{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ dispatch.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ dispatch.date ? dispatch.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dispatch_index') }}">retour à la liste </a>

    <a href="{{ path('dispatch_edit', {'id': dispatch.id}) }}">modifier</a>

    {{ include('dispatch/_delete_form.html.twig') }}
{% endblock %}
