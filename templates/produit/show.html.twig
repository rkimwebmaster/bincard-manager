{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}
{% block titre %}
	Détail produit 
{% endblock %}
     {% block lesboutons %}
	<a  href="{{ path('produit_index') }}" title="Retour à la liste " class="btn btn-info">
		<i class="fa fa-reply-all"></i>
	</a>
    	<a  href="{{ path('produit_edit', {'id': produit.id}) }}" title="Retour à la liste " class="btn btn-info">
		<i class="fa fa-edit"></i>
	</a>
{% endblock %}
	{% block lesite %} <span class="fa fa-angle-double-right"> </span> Config.  
						{% endblock %}
{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>PN</th>
                <td>{{ produit.pn }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit.description }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ produit.quantite }}</td>
            </tr>
            <tr>
                <th>Prix de Vente</th>
                <td>{{ produit.prixVente }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
